REGISTER QUERY <summary_PT1H_linear_tumbling_2> {
    FROM STREAM <linear-test-dataset>
    STARTING FROM Earliest
    WITH WINDOW RANGE PT1H
    MATCH (p:Person)-[r:IS_WITH]->(p2:Person) RETURN p,r,p2;
    EMIT ON EXIT
    EVERY PT1H
    INTO <summary_PT1H_linear_tumbling_2>
}
