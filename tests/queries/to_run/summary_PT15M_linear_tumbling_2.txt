REGISTER QUERY <summary_PT15M_linear_tumbling_2> {
    FROM STREAM <linear-test-dataset>
    STARTING FROM Earliest
    WITH WINDOW RANGE PT15M
    MATCH (p:Person)-[r:IS_WITH]->(p2:Person) RETURN p,r,p2;
    EMIT ON EXIT
    EVERY PT15M
    INTO <summary_PT15M_linear_tumbling_2>
}
