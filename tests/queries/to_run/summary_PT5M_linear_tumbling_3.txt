REGISTER QUERY <summary_PT5M_linear_tumbling_3> {
    FROM STREAM <linear-test-dataset>
    STARTING FROM Earliest
    WITH WINDOW RANGE PT5M
    MATCH (p:Person)-[r:IS_WITH]->(p2:Person) RETURN p,r,p2;
    EMIT ON EXIT
    EVERY PT5M
    INTO <summary_PT5M_linear_tumbling_3>
}
