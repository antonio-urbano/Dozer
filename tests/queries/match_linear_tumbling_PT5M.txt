REGISTER QUERY <match_linear_tumbling_PT5M> {
    FROM STREAM <star-test-dataset>
    STARTING FROM Earliest
    WITH WINDOW RANGE PT5M
    MATCH (p:Person)-[r:IS_WITH]->(p2:Person) RETURN p,r,p2;
    EMIT ON EXIT
    EVERY PT5M
    INTO <match_linear_tumbling_PT5M>
}
