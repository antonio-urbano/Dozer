REGISTER QUERY <match_linear_tumbling_PT1M> {
    FROM STREAM <test-dataset-small>
    STARTING FROM Earliest
    WITH WINDOW RANGE PT1M
    MATCH (p:Person)-[r:IS_WITH]->(p2:Person) RETURN p,r,p2;
    EMIT ON EXIT
    EVERY PT1M
    INTO <match_linear_tumbling_PT1M>
}
